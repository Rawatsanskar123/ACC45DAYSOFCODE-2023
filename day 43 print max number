#include<iostream>
#include<math.h>
int main(){
    int a ,b ;
    printf("enter any number");
    scanf("%d%d" , &a, &b);

    if(a>b) printf("max is %d\n", a);
    else printf("max is %d\n" , b);
    return 0;
}
